diff --git a/hindsight_api/_vector_index.py b/hindsight_api/_vector_index.py
index b850eafe6..e339bb1aa 100644
--- a/hindsight_api/_vector_index.py
+++ b/hindsight_api/_vector_index.py
@@ -12,7 +12,7 @@ from ._pg_extensions import create_extension
 logger = logging.getLogger(__name__)

 # Extensions a user can set via HINDSIGHT_API_VECTOR_EXTENSION.
-CONFIGURABLE_EXTENSIONS = ("pgvector", "pgvectorscale", "vchord", "scann")
+CONFIGURABLE_EXTENSIONS = ("pgvector", "pgvectorscale", "vchord", "scann", "lakebase_vector")

 # Extensions detect_vector_extension() can return. pg_diskann is a runtime-only
 # resolution from a configured "pgvectorscale" backend on Azure (uses a different
@@ -26,6 +26,7 @@ SCANN_MIN_ROWS_FOR_AUTO_INDEX = 10_000


 _EXTENSION_NAMES = {
+    "lakebase_vector": "lakebase_vector",
     "pgvector": "vector",
     "pgvectorscale": "vectorscale",
     "vchord": "vchord",
@@ -33,6 +34,7 @@ _EXTENSION_NAMES = {
 }

 _INDEX_USING_CLAUSES = {
+    "lakebase_vector": "USING lakebase_ann (embedding vector_cosine_ops)",
     "pgvector": "USING hnsw (embedding vector_cosine_ops)",
     "pgvectorscale": "USING diskann (embedding vector_cosine_ops) WITH (num_neighbors = 50)",
     "pg_diskann": "USING diskann (embedding vector_cosine_ops) WITH (max_neighbors = 50)",
@@ -41,6 +43,7 @@ _INDEX_USING_CLAUSES = {
 }

 _INDEX_TYPE_KEYWORDS = {
+    "lakebase_vector": "lakebase_ann",
     "pgvector": "hnsw",
     "pgvectorscale": "diskann",
     "pg_diskann": "diskann",
@@ -130,9 +133,11 @@ def ann_max_scan_tuples() -> int:
 # profile pins it off. Both profiles set it explicitly rather than relying on the
 # server default, so neither depends on what the other last left on the connection.
 _ANN_TUNING_LOW_LATENCY: dict[str, tuple[tuple[str, str], ...]] = {
+    "lakebase_vector": (("lakebase_ann.prefilter", "on"),),
     "pgvector": (("hnsw.ef_search", "60"), ("hnsw.iterative_scan", "off")),
 }
 _ANN_TUNING_HIGH_RECALL: dict[str, tuple[tuple[str, str], ...]] = {
+    "lakebase_vector": (("lakebase_ann.prefilter", "on"),),
     "pgvector": (
         ("hnsw.ef_search", "200"),
         ("hnsw.iterative_scan", "strict_order"),
@@ -145,6 +150,7 @@ _ANN_TUNING_HIGH_RECALL: dict[str, tuple[tuple[str, str], ...]] = {
 # Creation goes through _pg_extensions.create_extension so every extension lands
 # in ``public`` even when migrations run with a tenant schema on the search_path.
 _EXTENSION_INSTALL_PLAN: dict[str, tuple[tuple[str, bool], ...]] = {
+    "lakebase_vector": (("lakebase_vector", True),),
     "pgvector": (("vector", False),),
     "pgvectorscale": (("vector", False), ("vectorscale", True)),
     "vchord": (("vchord", True),),
@@ -152,6 +158,7 @@ _EXTENSION_INSTALL_PLAN: dict[str, tuple[tuple[str, bool], ...]] = {
 }

 _INSTALL_HINTS = {
+    "lakebase_vector": "CREATE EXTENSION lakebase_vector WITH SCHEMA public CASCADE;",
     "pgvector": "CREATE EXTENSION vector;",
     "pgvectorscale": "CREATE EXTENSION vector; then CREATE EXTENSION vectorscale CASCADE; (or pg_diskann on Azure)",
     "vchord": "CREATE EXTENSION vchord CASCADE;",
diff --git a/hindsight_api/config.py b/hindsight_api/config.py
index 20436ec76..3e6c4663e 100644
--- a/hindsight_api/config.py
+++ b/hindsight_api/config.py
@@ -1444,7 +1444,7 @@ DEFAULT_ANN_MAX_SCAN_TUPLES = 4000
 # tests can enumerate the family rather than hardcoding a copy that drifts — a
 # backend added here but forgotten in one of the arm builders is exactly how
 # `min_scores.keyword` became a no-op on four of them (#3882).
-VALID_TEXT_SEARCH_EXTENSIONS = ("native", "vchord", "pg_textsearch", "pgroonga", "pg_search")
+VALID_TEXT_SEARCH_EXTENSIONS = ("native", "vchord", "pg_textsearch", "pgroonga", "pg_search", "lakebase_text")
 DEFAULT_TEXT_SEARCH_EXTENSION = "native"

 # PostgreSQL text search dictionary used by the native tsvector backend. Only
@@ -3898,6 +3898,10 @@ class HindsightConfig:
                 f"Must be one of: {', '.join(VALID_TEXT_SEARCH_EXTENSIONS)}"
             )

+        if self.text_search_extension == "lakebase_text" or self.vector_extension == "lakebase_vector":
+            if self.database_backend != "postgresql":
+                raise ValueError("Lakebase search requires database_backend=postgresql")
+
         # Validate text_search_extension_native_language as a PG identifier.
         # Embedded directly into raw SQL via to_tsvector('<lang>', ...), so we
         # reject anything that isn't a plain identifier to prevent injection.
diff --git a/hindsight_api/db_url.py b/hindsight_api/db_url.py
index 0df4ac35d..5b5f2709e 100644
--- a/hindsight_api/db_url.py
+++ b/hindsight_api/db_url.py
@@ -47,6 +47,11 @@ def to_libpq_url(url: str) -> str:
     if not url or "://" not in url:
         return url

+    from hindsight_lakebase.databricks import lakebase_database_auth_enabled, refresh_lakebase_database_url
+
+    if lakebase_database_auth_enabled(database_url=url):
+        url = refresh_lakebase_database_url(url)
+
     parts = urlsplit(url)
     if parts.scheme not in _POSTGRES_SCHEMES:
         return url
diff --git a/hindsight_api/engine/consolidation/consolidator.py b/hindsight_api/engine/consolidation/consolidator.py
index 565bf19f1..267dbac3a 100644
--- a/hindsight_api/engine/consolidation/consolidator.py
+++ b/hindsight_api/engine/consolidation/consolidator.py
@@ -30,6 +30,7 @@ from itertools import combinations
 from typing import TYPE_CHECKING, Any, Literal

 import asyncpg
+from hindsight_lakebase.text import refresh_rows
 from pydantic import AliasChoices, BaseModel, ConfigDict, Field, ValidationError, field_validator

 from ...config import get_config
@@ -428,6 +429,8 @@ async def _apply_dedup_create_fold(
             source_bounds.occurred_end,
             source_bounds.mentioned_at,
         )
+        if folded is not None:
+            await refresh_rows(conn, fq_table("memory_units"), bank_id, [folded])
         if folded is None:
             # The twin vanished (or was rewritten) during the connection-free LLM window.
             # Don't skip the CREATE: returning None lets the caller insert the observation
@@ -528,6 +531,8 @@ async def _apply_dedup_update_fold(
             updated_text,
             live_u_sources,
         )
+        if folded is not None:
+            await refresh_rows(conn, fq_table("memory_units"), bank_id, [folded])
         if folded is None:
             # Twin or updated row vanished during the LLM window — keep the updated row
             # as a distinct observation instead of deleting it unfolded.
@@ -2734,6 +2739,8 @@ async def _apply_update_action(
             source_bounds.mentioned_at,
             merged_tags,
         )
+        if updated_rows:
+            await refresh_rows(conn, fq_table("memory_units"), bank_id, [uuid.UUID(observation_id)])
         # The source-liveness checks above guard the *source* memories; the
         # observation row itself (WHERE id = $5) can still be invalidated/deleted
         # concurrently, matching 0 rows. Bail out BEFORE the observation_history
@@ -3405,6 +3412,7 @@ async def _apply_create_observation(
             obs_occurred_end,
             obs_mentioned_at,
         )
+        await refresh_rows(conn, fq_table("memory_units"), bank_id, [observation_id])
         created_id = row["id"]

         # Populate observation_sources junction table (Oracle only — PG uses native array ops).
diff --git a/hindsight_api/engine/db/ops_postgresql.py b/hindsight_api/engine/db/ops_postgresql.py
index 578351944..977cf2463 100644
--- a/hindsight_api/engine/db/ops_postgresql.py
+++ b/hindsight_api/engine/db/ops_postgresql.py
@@ -7,6 +7,8 @@ efficient batch operations.
 import asyncio
 from datetime import datetime

+from hindsight_lakebase.text import indexed_write, refresh_rows
+
 from .base import DatabaseConnection
 from .ops import (
     ChunkIdOwnedByAnotherBank,
@@ -145,6 +147,7 @@ class PostgreSQLOps(DataAccessOps):
             bank_id,
         )

+    @indexed_write
     async def insert_facts_batch(
         self,
         conn: DatabaseConnection,
@@ -232,6 +235,7 @@ class PostgreSQLOps(DataAccessOps):
             text_signals_list,
             attachment_ids_list,
         )
+        await refresh_rows(conn, table, bank_id, [row["id"] for row in results])
         return [str(row["id"]) for row in results]

     async def delete_unit_links(
diff --git a/hindsight_api/engine/db/postgresql.py b/hindsight_api/engine/db/postgresql.py
index 8c1657774..9a0aa2c2c 100644
--- a/hindsight_api/engine/db/postgresql.py
+++ b/hindsight_api/engine/db/postgresql.py
@@ -297,6 +297,14 @@ class PostgreSQLBackend(DatabaseBackend):
         else:
             pool_setup = _application_name_setup(app_name, None) if app_name else None

+        connect_kwargs: dict[str, Any] = {}
+        from hindsight_lakebase.databricks import lakebase_database_auth_enabled
+
+        if lakebase_database_auth_enabled(database_url=dsn):
+            from hindsight_lakebase.databricks import lakebase_database_password_async
+
+            connect_kwargs["password"] = lakebase_database_password_async
+
         # Skipping asyncpg's release-time reset removes one Postgres round trip
         # per acquire/release cycle (and, behind a transaction-mode pooler, one
         # server-side transaction): measured as 1 of ~5 round trips on a
@@ -312,6 +320,7 @@ class PostgreSQLBackend(DatabaseBackend):
             init=pool_init,
             setup=pool_setup,
             connection_class=_NoResetConnection,
+            **connect_kwargs,
         )
         logger.info(
             f"PostgreSQL pool created (min={min_size}, max={max_size}, "
diff --git a/hindsight_api/engine/embeddings.py b/hindsight_api/engine/embeddings.py
index c98bab910..b9f7a1a1e 100644
--- a/hindsight_api/engine/embeddings.py
+++ b/hindsight_api/engine/embeddings.py
@@ -975,6 +975,30 @@ class OpenAIEmbeddings(Embeddings):
         return [e.embedding for e in sorted(response.data, key=lambda x: x.index)]


+class DatabricksEmbeddings(OpenAIEmbeddings):
+    """OpenAI-compatible embeddings authenticated as the Databricks App."""
+
+    def __init__(self, model, base_url=None, **kwargs):
+        from hindsight_lakebase.databricks import DatabricksOAuthTokenProvider
+
+        self._databricks_auth = DatabricksOAuthTokenProvider.from_env()
+        super().__init__(
+            api_key=self._databricks_auth.get_token_async,
+            model=model,
+            base_url=self._databricks_auth.resolve_ai_gateway_base_url(base_url),
+            **kwargs,
+        )
+
+    def _loop_client(self) -> Any:
+        # AsyncOpenAI refreshes the token from its provider before each request.
+        assert self._clients is not None
+        return self._clients.get()
+
+    @property
+    def provider_name(self) -> str:
+        return "databricks"
+
+
 class CodexOAuthEmbeddings(OpenAIEmbeddings):
     """
     OpenAI embeddings using the Codex/ChatGPT OAuth token from the Codex
@@ -2149,6 +2173,18 @@ def create_embeddings_from_env() -> Embeddings:
             batch_size=config.embeddings_onnx_batch_size,
             cpu_mem_arena=config.embeddings_onnx_cpu_mem_arena,
         )
+    elif provider == "databricks":
+        return _with_request_concurrency(
+            DatabricksEmbeddings(
+                model=config.embeddings_openai_model,
+                base_url=config.embeddings_openai_base_url,
+                batch_size=config.embeddings_openai_batch_size,
+                dimensions=config.embeddings_openai_dimensions,
+                query_prefix=query_prefix,
+                passage_prefix=passage_prefix,
+            ),
+            config,
+        )
     elif provider == "openai":
         # Use dedicated embeddings API key, or fall back to LLM API key
         api_key = config.embeddings_openai_api_key
@@ -2311,6 +2347,6 @@ def create_embeddings_from_env() -> Embeddings:
     else:
         raise ValueError(
             f"Unknown embeddings provider: {provider}. "
-            f"Supported: 'local', 'onnx', 'tei', 'openai', 'openai-codex', 'openrouter', 'requesty', 'cohere', 'google', "
+            f"Supported: 'local', 'onnx', 'tei', 'databricks', 'openai', 'openai-codex', 'openrouter', 'requesty', 'cohere', 'google', "
             f"'zeroentropy', 'litellm', 'litellm-sdk'"
         )
diff --git a/hindsight_api/engine/llm_wrapper.py b/hindsight_api/engine/llm_wrapper.py
index 76575260f..537315fd8 100644
--- a/hindsight_api/engine/llm_wrapper.py
+++ b/hindsight_api/engine/llm_wrapper.py
@@ -780,6 +780,7 @@ def create_llm_provider(
         )

     elif provider_lower in (
+        "databricks",
         "openai",
         "groq",
         "ollama",
@@ -962,6 +963,7 @@ class LLMProvider:

         # Validate provider
         valid_providers = [
+            "databricks",
             "openai",
             "openai-responses",
             "groq",
diff --git a/hindsight_api/engine/memories/pg/writes.py b/hindsight_api/engine/memories/pg/writes.py
index 20d4598c9..51be5eee0 100644
--- a/hindsight_api/engine/memories/pg/writes.py
+++ b/hindsight_api/engine/memories/pg/writes.py
@@ -19,6 +19,8 @@ import uuid
 from collections.abc import Callable
 from typing import TYPE_CHECKING, Any

+from hindsight_lakebase.text import refresh_rows
+
 from ....config import get_config
 from ..base import StoredMemory

@@ -474,6 +476,7 @@ async def restore_memory(*, conn, fq_table, bank_id: str, unit_id: str) -> Store
         str(unit_id),
         bank_id,
     )
+    await refresh_rows(conn, mu, bank_id, [str(unit_id)])
     # Rebuild search_vector with the *current* backend, so a backend change while
     # the fact sat archived cannot leave a stale/wrong-type vector (#2503). None
     # means the backend indexes base columns directly and leaves it empty.
@@ -592,6 +595,7 @@ async def apply_edit(
         """,
         *params,
     )
+    await refresh_rows(conn, mu, bank_id, [str(unit_id)])
     # Drop only the DERIVED links — graph maintenance recomputes temporal/semantic. Causal edges
     # are retain-time extraction output that nothing recreates, so an edit preserves them (#2864).
     # In lock order, like every other link delete (see delete_unit_links).
diff --git a/hindsight_api/engine/memory_engine.py b/hindsight_api/engine/memory_engine.py
index 100357a9a..f1d7b2d94 100644
--- a/hindsight_api/engine/memory_engine.py
+++ b/hindsight_api/engine/memory_engine.py
@@ -29,6 +29,7 @@ from datetime import UTC, datetime, timedelta, timezone
 from typing import TYPE_CHECKING, Any, Literal, NoReturn, ParamSpec, TypeVar, cast

 import asyncpg
+from hindsight_lakebase.text import refresh_rows, search_scope, write_scope
 from pydantic import ValidationError

 from .._vector_index import (
@@ -16916,6 +16917,7 @@ class MemoryEngine(MemoryEngineInterface):
             json.dumps(trigger) if trigger else None,
             json.dumps(document.structure.model_dump()),
         )
+        await refresh_rows(conn, fq_table("mental_models"), bank_id, [mental_model_id])
         assert row is not None
         return row

@@ -18403,7 +18405,7 @@ class MemoryEngine(MemoryEngineInterface):

         # The exit stack carries the row lock a trigger patch takes below; it stays
         # empty (and free) on every other update.
-        async with use_or_acquire(backend, conn) as conn, AsyncExitStack() as stack:
+        async with use_or_acquire(backend, conn) as conn, write_scope(conn), AsyncExitStack() as stack:
             # A supplied trigger PATCHES the stored one (see _merge_trigger): callers
             # send only the fields they set, so the rest have to be read back rather
             # than defaulted. Only paid for when a trigger is actually being changed.
@@ -18565,6 +18567,8 @@ class MemoryEngine(MemoryEngineInterface):
             """

             row = await conn.fetchrow(query, *params)
+            if row is not None and (name is not None or content is not None):
+                await refresh_rows(conn, fq_table("mental_models"), bank_id, [mental_model_id])

             # Persist the previous-version snapshot in the dedicated history table
             # (one row per refresh), then trim to the configured cap. Replaces the
@@ -18770,7 +18774,7 @@ class MemoryEngine(MemoryEngineInterface):
             else None
         )
         sv_clause = f", search_vector = {sv_expr}" if sv_expr else ""
-        async with acquire_with_retry(backend) as conn:
+        async with acquire_with_retry(backend) as conn, write_scope(conn):
             row = await conn.fetchrow(
                 f"""
                 UPDATE {fq_table("mental_models")}
@@ -18791,6 +18795,7 @@ class MemoryEngine(MemoryEngineInterface):
             # findable by title, rather than leaving the index describing content that no longer
             # exists.
             if row is not None:
+                await refresh_rows(conn, fq_table("mental_models"), bank_id, [mental_model_id])
                 vec = None
                 if self._knowledge_index_store(bank_id) is not None:
                     vec = await self._mental_model_embedding_vector(row["name"] or "", "")
@@ -19442,7 +19447,7 @@ class MemoryEngine(MemoryEngineInterface):
             return []

         backend = await self._get_backend()
-        async with acquire_with_retry(backend) as conn:
+        async with acquire_with_retry(backend) as conn, search_scope(conn, fetch, active=include_bm25):
             if emb_str is not None and not include_bm25:
                 # Vector-only: no BM25 arm to fuse with, so rank straight off the ANN scan.
                 sql = f"""
diff --git a/hindsight_api/engine/provider_auth.py b/hindsight_api/engine/provider_auth.py
index 9b077bab0..f1b117772 100644
--- a/hindsight_api/engine/provider_auth.py
+++ b/hindsight_api/engine/provider_auth.py
@@ -23,6 +23,7 @@ _PROVIDERS_WITHOUT_API_KEY = frozenset(
         "litellm",
         "litellmrouter",
         "bedrock",
+        "databricks",
         "nous",
         "xai-oauth",
     }
diff --git a/hindsight_api/engine/providers/openai_compatible_llm.py b/hindsight_api/engine/providers/openai_compatible_llm.py
index ae5952f21..e1ca4d5d0 100644
--- a/hindsight_api/engine/providers/openai_compatible_llm.py
+++ b/hindsight_api/engine/providers/openai_compatible_llm.py
@@ -792,6 +792,7 @@ class OpenAICompatibleLLM(LLMInterface):

         # Validate provider
         valid_providers = [
+            "databricks",
             "openai",
             "groq",
             "ollama",
@@ -812,6 +813,13 @@ class OpenAICompatibleLLM(LLMInterface):
         if self.provider not in valid_providers:
             raise ValueError(f"OpenAICompatibleLLM only supports: {', '.join(valid_providers)}. Got: {self.provider}")

+        self._databricks_auth = None
+        if self.provider == "databricks":
+            from hindsight_lakebase.databricks import DatabricksOAuthTokenProvider
+
+            self._databricks_auth = DatabricksOAuthTokenProvider.from_env()
+            self.base_url = self._databricks_auth.resolve_ai_gateway_base_url(self.base_url)
+
         # Set default base URLs
         if not self.base_url:
             if self.provider == "groq":
@@ -892,7 +900,7 @@ class OpenAICompatibleLLM(LLMInterface):

         # Create OpenAI client — extract query params from base_url (e.g. Azure api-version)
         client_kwargs: dict[str, Any] = {
-            "api_key": self.api_key,
+            "api_key": self._databricks_auth.get_token_async if self._databricks_auth else self.api_key,
             "max_retries": 0,
             "default_headers": with_openai_compatible_user_agent(default_headers),
         }
diff --git a/hindsight_api/engine/search/retrieval.py b/hindsight_api/engine/search/retrieval.py
index 373ecc9bf..1dcd33eed 100644
--- a/hindsight_api/engine/search/retrieval.py
+++ b/hindsight_api/engine/search/retrieval.py
@@ -14,6 +14,8 @@ from dataclasses import dataclass, field
 from datetime import UTC, datetime
 from typing import TYPE_CHECKING, Any, Optional

+from hindsight_lakebase.text import indexed_search
+
 from ...config import get_config
 from ..db.ops import UpdatedWindow
 from ..memory_engine import fq_table
@@ -120,6 +122,7 @@ def set_default_graph_retriever(retriever: GraphRetriever | None) -> None:
     _default_graph_retriever = retriever


+@indexed_search
 async def retrieve_semantic_bm25_combined_sql(
     conn,
     query_emb_str: str,
diff --git a/hindsight_api/engine/sql/postgresql.py b/hindsight_api/engine/sql/postgresql.py
index de5c9f13d..6a2fe7c94 100644
--- a/hindsight_api/engine/sql/postgresql.py
+++ b/hindsight_api/engine/sql/postgresql.py
@@ -7,6 +7,10 @@ and other non-portable patterns.

 from dataclasses import dataclass

+from hindsight_lakebase.text import distance as lakebase_distance
+from hindsight_lakebase.text import index_name as lakebase_index_name
+from hindsight_lakebase.tokenizer import search_text as lakebase_search_text
+
 from ..._text_search import mental_models_text_document
 from .base import SQLDialect, bm25_score_gate

@@ -96,6 +100,9 @@ def knowledge_bm25_arm(
     """
     a = table_alias
     p = text_param
+    if text_search_extension == "lakebase_text":
+        expr = lakebase_distance(f"{a}.search_vector", p, lakebase_index_name("mental_models"))
+        return KnowledgeBm25Arm(order_by=f"{expr} ASC", match_filter=f"AND -({expr}) > 0")

     if text_search_extension == "vchord":
         # VectorChord BM25 over the bm25vector search_vector column, identical to
@@ -360,7 +367,13 @@ class PostgreSQLDialect(SQLDialect):
         # outer wrapper below instead.
         floor_in_where = False

-        if text_search_extension == "vchord":
+        if text_search_extension == "lakebase_text":
+            distance = lakebase_distance("search_vector", text_param, lakebase_index_name(table))
+            bm25_score_expr = f"-({distance})"
+            bm25_order_by = f"{distance} ASC"
+            bm25_where_filter = f"AND {bm25_score_expr} {bm25_score_gate(bm25_min_score)}"
+            floor_in_where = True
+        elif text_search_extension == "vchord":
             # <&> returns the NEGATIVE BM25 score (lower = more relevant), negate
             # for a positive score where higher = more relevant.
             bm25_score_expr = f"-(search_vector <&> to_bm25query('idx_memory_units_text_search', tokenize({text_param}, 'llmlingua2')))"
@@ -456,6 +469,8 @@ class PostgreSQLDialect(SQLDialect):
         text_search_extension: str = "native",
         max_query_terms: int | None = None,
     ) -> str:
+        if text_search_extension == "lakebase_text":
+            return lakebase_search_text(query_text, max_query_terms)
         if text_search_extension in ("vchord", "pg_textsearch", "pg_search", "pgroonga"):
             return query_text
         if max_query_terms is not None and max_query_terms > 0:
diff --git a/hindsight_api/engine/transfer/importer.py b/hindsight_api/engine/transfer/importer.py
index 7469576b4..f150bb0f2 100644
--- a/hindsight_api/engine/transfer/importer.py
+++ b/hindsight_api/engine/transfer/importer.py
@@ -19,6 +19,8 @@ from dataclasses import dataclass, field
 from datetime import UTC, date, datetime
 from typing import TYPE_CHECKING, Any, Literal

+from hindsight_lakebase.text import indexed_import
+
 from ..causal_links import CANONICAL_CAUSAL_LINK_TYPE, LEGACY_CAUSAL_LINK_TYPES
 from ..db.ops_postgresql import pg_search_vector_expr
 from ..db_utils import acquire_with_retry
@@ -461,6 +463,7 @@ def _resolve_bank_rows_json_encoding(manifest: TransferManifest) -> BankRowsJSON
     return manifest.bank_rows_json_encoding or "decoded"


+@indexed_import
 async def _restore_rows(
     conn: Any,
     table: str,
diff --git a/hindsight_api/engine/vector_index_health.py b/hindsight_api/engine/vector_index_health.py
index 83bd97666..4f5548976 100644
--- a/hindsight_api/engine/vector_index_health.py
+++ b/hindsight_api/engine/vector_index_health.py
@@ -65,6 +65,7 @@ _SUPPORTED_INDEX_AM: tuple[str, ...] = (
     "ivfflat",
     "diskann",
     "vchordrq",
+    "lakebase_ann",
 )


diff --git a/hindsight_api/migrations.py b/hindsight_api/migrations.py
index e794eb924..ca64e12e4 100644
--- a/hindsight_api/migrations.py
+++ b/hindsight_api/migrations.py
@@ -118,7 +118,7 @@ def _ensure_pgvector_extension_in_public(conn: Connection) -> None:

 def _bootstrap_vector_extension_for_migrations(conn: Connection, vector_extension: str) -> None:
     """Bootstrap the configured vector backend before schema migrations run."""
-    if vector_extension == "pgvector":
+    if vector_extension in ("pgvector", "lakebase_vector"):
         _ensure_pgvector_extension_in_public(conn)
     bootstrap_extension(conn, vector_extension)
     # Repair anything an older version installed into a tenant schema, where the
@@ -159,7 +159,7 @@ def _vector_index_names(
             WHERE n.nspname = :schema
               AND t.relname = :table
               AND a.attname = 'embedding'
-              AND (NOT :vector_ams_only OR am.amname IN ('hnsw', 'vchordrq', 'diskann', 'scann'))
+              AND (NOT :vector_ams_only OR am.amname IN ('hnsw', 'vchordrq', 'diskann', 'scann', 'lakebase_ann'))
               AND (:name_like IS NULL OR i.relname LIKE :name_like)
         """),
         {
@@ -217,6 +217,13 @@ def _run_migrations_internal(database_url: str, script_location: str, schema: st
         script_location: Path to alembic scripts
         schema: Target schema (None for default/public)
     """
+    from .config import get_config
+
+    if get_config().text_search_extension == "lakebase_text" or get_config().vector_extension == "lakebase_vector":
+        from hindsight_lakebase.migrations import historical_migrations
+
+        historical_migrations(database_url, script_location, schema)
+        return
     schema_name = schema or "public"
     logger.info(f"Running database migrations to head for schema '{schema_name}'...")
     logger.info(f"Database URL: {mask_network_location(database_url)}")
@@ -572,6 +579,10 @@ def _migrate_table_embedding_dimension(
         logger.info(f"Changed {table_name}.embedding dimension to {required_dimension} (no vector index)")
         return

+    if vector_ext == "lakebase_vector" and table_name == "memory_units":
+        # Bank-scoped indexes belong to the existing maintenance sweep.
+        return
+
     _create_embedding_vector_index(conn, schema_name, table_name, required_dimension, vector_ext, row_count)
     logger.info(f"Successfully changed {table_name}.embedding dimension to {required_dimension}")

@@ -736,6 +747,9 @@ def ensure_vector_extension(
         if store_owned_memories:
             tables_to_check = [entry for entry in tables_to_check if entry[0] != "memory_units"]

+        if target_ext == "lakebase_vector":
+            tables_to_check.append(("mental_models", "idx_mental_models_embedding"))
+
         target_index_type = index_type_keyword(target_ext)

         mismatched_tables = []
@@ -816,7 +830,9 @@ def ensure_vector_extension(
                 continue

             indexdef = current_index_info[0].lower()
-            if "scann" in indexdef:
+            if "lakebase_ann" in indexdef:
+                current_index_type = "lakebase_ann"
+            elif "scann" in indexdef:
                 current_index_type = "scann"
             elif "diskann" in indexdef:
                 current_index_type = "diskann"
@@ -1082,6 +1098,11 @@ def ensure_text_search_extension(
     Raises:
         RuntimeError: If extension mismatch with existing data
     """
+    if text_search_extension == "lakebase_text":
+        from hindsight_lakebase.migrations import ensure_text
+
+        ensure_text(database_url, schema)
+        return
     schema_name = schema or "public"
     pg_search_tokenizer = normalize_pg_search_tokenizer(pg_search_tokenizer)

diff --git a/hindsight_api/pg0.py b/hindsight_api/pg0.py
index 5df3693d6..7a3ee24d5 100644
--- a/hindsight_api/pg0.py
+++ b/hindsight_api/pg0.py
@@ -219,4 +219,8 @@ async def resolve_database_url(db_url: str) -> str:
             kwargs["password"] = parsed.password
         pg0 = EmbeddedPostgres(**kwargs)
         return await pg0.ensure_running()
+    from hindsight_lakebase.databricks import lakebase_database_auth_enabled, refresh_lakebase_database_url
+
+    if lakebase_database_auth_enabled(database_url=db_url):
+        return refresh_lakebase_database_url(db_url)
     return db_url
