import SwiftUI

enum CustomerIntelligenceCustomerCardLayout {
    static let spacing: CGFloat = 16
    static let columns = [
        GridItem(.adaptive(minimum: 260, maximum: 380), spacing: spacing),
    ]
}
